import 'package:flutter/material.dart';

//constant radius different borders
const BorderRadius radius5 = BorderRadius.all(Radius.circular(5));
const BorderRadius radius10 = BorderRadius.all(Radius.circular(10));
const BorderRadius radius20 = BorderRadius.all(Radius.circular(20));

// constant sizings for required spaces
const height10 = SizedBox(height: 10);
const height20 = SizedBox(height: 20);
const width10 = SizedBox(width: 10);
const width20 = SizedBox(width: 20);